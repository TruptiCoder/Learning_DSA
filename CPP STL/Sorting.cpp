#include<bits/stdc++.h>
using namespace std;

int main() {
    // sort(a , a + n);
    // sort(v.begin(), v.end());
    // sort(a+2, a+4);
    // sort(a, a+n, greater<int>);

    // pair<int, int> a[] = {{1, 2}, {2, 1}, {4, 1}};

    // // sort it according to second element if second element is same, then sort it according to first element but in descending

    // sort(a, a+n, comp);
    // {4, 1}, {2, 1}, {1, 2}

    // int num = 7;
    // int cnt = _builtin_popcount();

    // long long num = 1942389723892;
    // int cnt = _builtin_popcount();
    
    // string s = "123";
    // do {
    //     cout << s << endl;
    // } while(next_permutation(s.begin(), s.end()));

    // return 0;
}